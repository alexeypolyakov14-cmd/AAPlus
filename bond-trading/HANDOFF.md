# Передача контекста: bondtrader (ВДО-портфель на MOEX)

Дата: 11.09.2026. Ветка разработки: `claude/rf-bond-trading-system-k7lvwh` (репозиторий `alexeypolyakov14-cmd/AAPlus`,
папка `bond-trading/`). `main` = состояние ветки на 14:24 МСК 11.09 (пуш в main только по прямой просьбе пользователя).
PR не создавать без просьбы. Секреты (T-Invest, Telegram) только в GitHub Secrets, в код и чат не попадают.

## Как работает пользователь
Пользователь не запускает команды сам: он пишет в чат, ассистент запускает через GitHub Actions и пересказывает
результат по-русски. Контейнер сессии без сети: MOEX/Telegram недоступны, всё живое — только через Actions.

Запуск разовой команды: `mcp__github__actions_run_trigger(run_workflow, workflow_id=bond-trading.yml, ref=<ветка>,
inputs={"cmd": "<аргументы bondtrader>"})` — job «Разовая команда под профилем hy», профиль `configs/hy.yaml`.
Вывод: `list_workflow_jobs` → job id → `get_job_logs(job_id, return_content=true, tail_lines=N)`.
`inputs.lookup="НЛМК"` = `bondtrader why`. Аргументы с пробелами в `cmd` не работают (word-split).
Другие галочки dispatch: `daily` (отчёт в Telegram), `bot` (+`bot_menu`), `telegram_setup`, `smoke` (долгий живой прогон, ~35 мин),
`execute` + strategy/profile (ребалансировка песочницы T-Invest).

Стоимость: репозиторий приватный, минуты Actions платные (пользователь поднял лимит 11.09). Долгий job live-smoke
запускается только по push в main, по расписанию или по галочке `smoke`.

## Что построено (кратко)
- Данные: MOEX ISS (все облигации TQOB/TQCB, кривая zcyc, описания), ЦБ (ключевая), книги: рейтинги
  `data/ratings.csv`, новости `data/news.csv` (RSS: Google News, Bing, Интерфакс, РБК, Коммерсант, Финам),
  отчётность ГИР БО и e-disclosure — доступны ТОЛЬКО из РФ (из CI геоблок), книги пустые.
- Конвейер: сито ликвидности (`screener.py`) → стоп-факторы (дефолты MOEX, новости) → риск-фильтр (`risk.py`,
  спред ≤ 2000 б.п., листинг ≤ 3, рейтинг ≥ B) → стратегия → лимиты портфеля → ордера.
- Стратегии: `value_hy` (композит: остаток спреда к регрессии 0.40, отчётность 0.35 (пока 0), доходность−PD·LGD 0.25,
  новости 0.15), `gspread` (rank=spread|peers|model; top_n, per_issuer, min_spread_bp, min_excess_bp) — сейчас
  пользователь работает с `gspread rank=peers`.
- Группа пиров `analytics/peers.py`: рейтинг ± дюрация ±1 год (опц. сектор), каскадное расширение при <5 пиров;
  команда `peers`, `peers "<бумага>"`.
- Ключ эмитента (`models.issuer_key`): по полному названию без ОПФ; «БалтЛизП16»/«БалтЛизП18» — один эмитент.
- Валюта бумаги = FACEUNIT (валютные/замещающие отсеиваются); корпоративные линкеры (номинал > первоначального,
  купон < 5%) отсеиваются как линкеры.
- Рейтинги: ЕДИНЫЙ ИСТОЧНИК — реестр Эксперт РА (`data.ratings_policy: primary`), НКР/АКРА только запасные.
  `ratings fetch` перезагружает реестры Эксперт РА/НКР целиком (без `--accumulate`), АКРА — пресс-релизы накопительно.
  Сопоставление названий строгое двустороннее (`issuer_same`), допускается одно лишнее слово субъекта.
  Диагностика: `ratings match "<имена через запятую>"`, `ratings audit --problems`, `ratings list`.
- Исполнение: бумажный брокер и песочница T-Invest (счёт 328b9a91-…; после разворота 11.09 — 31 позиция под value_hy,
  NAV ~2.46 млн). Боевой контур только с `--broker tinvest --live --confirm`.
- Telegram: бот @LTT_14092000_bot, chat_id в секрете; ежедневный HTML-отчёт (`report_tg.py`), кнопки бота
  (`bot.py`: отчёт/позиции/новости/скрин/алерты; отвечает через job `bot`).
- Расписание GitHub (10:40 МСК отчёт, каждые 10 мин бот) на main НЕ СРАБАТЫВАЛО 11.09 ни разу; поставлен Routine
  «Ежедневный отчёт bondtrader (страховка…)» на 10:36 МСК по будням (fires в сессию 01UDyTPXut64rqna79L9XbjY).

## В работе на момент передачи
1. Запущен dispatch `ratings fetch --sources raexpert nkr` (полная перезагрузка реестров, книга сохраняется в кэш
   Actions `books-<run_id>`). Дальше нужно последовательно: `ratings audit --problems`, `peers --top 40 --min-excess 100`,
   `signals -s gspread -p rank=peers -p min_excess_bp=100 -p min_spread_bp=400 -p top_n=20 -p per_issuer=1 -p max_duration=3`
   и отчитаться: покрытие рейтингами Эксперт РА / запасными / без рейтинга, замечания аудита, топ-20 по превышению
   над пирами (бумага, рейтинг, YTW, спред, медиана пиров, превышение, n, новости), целевой список.
2. Открытый вопрос по данным: Балтийский лизинг в реестре Эксперт РА AA- (28.01.2026), а новости говорят о понижении
   и спред 1900 б.п. — проверить после перезагрузки реестра.
3. Пионер-Лизинг, Уралкуз, Страна в книге отсутствуют (раньше получали чужие рейтинги) — теперь честно «без рейтинга».

## Дальше по плану пользователя
- Сначала довести сравнение с группой пиров (готово, идёт проверка на живых данных), потом «перебор методологии»:
  кривая самого эмитента (разброс спредов между выпусками одного эмитента), спред vs собственная история (z-оценка),
  запаздывание рейтинга (повышение без сжатия спреда), первичка/первые недели после размещения, оферта как опцион,
  расхождение с YTM MOEX; отчётность из ГИР БО — только когда пользователь прогонит `financials fetch --from-screen`
  и `disclosure fetch --from-screen` локально из РФ и закоммитит книги.
- Лимит 4% на бумагу в `configs/hy.yaml` режет портфели из 10–20 имён (инвестировано 40–77%) — поднять до 5–10%,
  когда определятся с числом позиций; `max_unrated_share 0.25` тоже режет (в peers-топе много бумаг без рейтинга).
- Пользователю важно объяснять, ПОЧЕМУ бумага в списке («неэффективность или рынок что-то знает»): новости,
  изменения рейтинга, оферты, листинг, оборот — колонка why в `spreads`, обоснование в `signals`.

## Полезные команды (все под `-c configs/hy.yaml`, в CI профиль задан через BONDTRADER_CONFIG)
`screen --top 40 -v`, `spreads --top 40 --bottom 10`, `peers --top 40 --min-excess 100`, `peers "БалтЛиз"`,
`signals -s gspread -p rank=peers …`, `signals -s value_hy`, `why "НЛМК"`, `bond <SECID> --schedule`,
`ratings audit --problems`, `ratings match "…"`, `news list --negative --top 30`, `report --broker tinvest --md r.md --tg-file r.html`,
`portfolio --broker tinvest`, `trade --broker tinvest -s gspread` (dry-run; `--confirm` исполняет в песочнице).
Тесты: `pytest -q` в `bond-trading/` (84 теста, офлайн на фикстурах).
